package com.test.demo;

import com.test.demo.model.*;

import java.util.ArrayList;
import java.util.List;

class ClassBuilder {
/*
    Country getCountry() {
        Country country = new CountryBuilder()
                .setId(1)
                .setName("india")
                .setPopulation(1000)
                .build();
        List<City> cities = new ArrayList<>();
        cities.add(getCity(country));
        Nationality nationality = getNationality(country);
/*
        country.setNationality(nationality);
        country.setCities(cities);

        return country;
    }

    private Nationality getNationality(Country country) {
        return new NationalityBuilder()
                .setId(1)
                .setCountryName("india")
                .setNationality("indians")
                .setCountry(country)
                .build();
    }

    private City getCity(Country country) {
        return new CityBuilder()
                .setId(1)
                .setName("kolkata")
                .setPin(713206)
                .setPopulation(200)
                .setCountry(country)
                .build();
    }
    */
}
